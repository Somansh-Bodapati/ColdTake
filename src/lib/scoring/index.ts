// Scoring engine entry point — placeholder for Session 1+.
// Pure function: no I/O, no Date.now(), no DB access (see CLAUDE.md rule 1).
// This is the engine's entry module, not a re-export barrel.

export {};
